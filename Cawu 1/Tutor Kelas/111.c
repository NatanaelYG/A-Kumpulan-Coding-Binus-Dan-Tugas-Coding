#include <stdio.h>
#include <string.h>

int main(){
	char s[30];
	int t;
	int i,j,k;
	
	scanf("%d", &t);
	for(i = 1;i <= t;i++){
		scanf("%s", &s);
		panjang = strlen(s);
		
		for(j = 0;j < panjang;j++){
			string[s[i]-'a'] = 1;
		}
		panjang = string(string);
		count++;
		for(j = 0;j < panjang;j++){
			string[s[i]-'a'] = 1;
		}
		
	}
}
